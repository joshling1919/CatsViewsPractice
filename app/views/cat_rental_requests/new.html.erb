<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>new rental_request</title>
  </head>
  <body>
    <form action="/cat_rental_requests" method="post">
      <label for="cat">Choose your cat:</label>
      <select name="cat_rental_request[cat_id]" id="cat">
        <option></option>
        <% Cat.all.each do |cat| %>
          <option value= <%= cat.id %>> <%= cat.name %>
          </option>
        <% end %>
      </select>

    <label for="start date">Start date</label>
    <input type="date" name= "cat_rental_request[start_date]" value= <%= @cat_rental_request.start_date %>>

    <label for="end date">End date</label>
    <input type="date" name= "cat_rental_request[end_date]" value= <%= @cat_rental_request.end_date %>>

      <input type="submit" value="Choose Cat">
    </form>

  </body>
</html>
